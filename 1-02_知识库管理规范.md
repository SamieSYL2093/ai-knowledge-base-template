# 知识库管理规范（meta）

> 用途：本知识库自己的编写规则——文件格式、编号体系、修改流程
> 关键词：meta、规范模板、头部格式、编号、新增文件、并发、开工必看、完工必查、立项
> 更新：2026-08-02

---

## 零、体系架构与核心原则（最高优先级，规则冲突时以本节为准）

### 五条核心原则

| # | 原则 | 含义 |
|---|------|------|
| 1 | **单一事实来源** | 一条知识只写在一个文件里，其他文件用引用/指针（"见 XX 文件"），不复制粘贴 |
| 2 | **地图与行李分离** | 知识库只存"地图"（规范+登记表），不存"行李"（代码/素材/生成物）；项目内容在各项目目录自治 |
| 3 | **Git 是唯一工作记录** | 做完事署名 commit 即完成留痕，不在任何地方手动重复记账 |
| 4 | **MD 是事实源，产物是打印件** | 由脚本生成的文件（HTML/导出/打包产物）一律改源文件重新生成，不手改、不同步维护两份内容 |
| 5 | **闭环必达** | 每个建议/待决项必须有明确去处：进池 → 进待决策 → 用户决策 → 删行+回写来源 |

### 机制的生命周期（开发期 → 成熟期）

部分机制是"脚手架"，成熟后应拆除或降级，避免体系越来越重：

- **🏗️ 脚手架**（如"规则会变"高频警示、临时巡检）：落地/稳定后即撤
- **🏠 卫生规则**（五条核心原则、Git 署名、文件>记忆）：**永久保留**——成本趋近于零，不拆

> 判断标准：机制的成本随体系稳定趋近于零的，保留；成本不减少的，到期拆除。

---

## 一、文件头部格式（必须）

每个文件开头必须是三行引用块，AI 靠它判断"该不该读这个文件"：

```markdown
> 用途：一句话说明这份文件管什么
> 关键词：检索用词，3-8 个，顿号分隔（含同义词/口语说法）
> 更新：YYYY-MM-DD（最后一次实质修改日期）
```

### 关键词怎么选
- 站在"提问的人"角度：他会用什么词找这个内容？
- 包含口语说法（如"辅导日志"的同时加上"辅导记录"、"辅导纪要"）
- 如果跟特定项目/客户相关，带上名称

---

## 二、文件编号

```
1-01 ~ 1-99        规则层    你的档案、管理规范、平台攻略、协作机制（根目录 1-xx 文件）
3-01 ~ 3-99        登记层    项目与技能登记表（根目录 3-xx 文件）
10-19_业务流程/    业务层    行业知识、工作流程、规范模板（目录内编号从 10 开始递增）
20-29_工具技能/    工具层    工具手册、AI 技能文档（目录内编号从 20 开始递增）
```

- 新文件取该区间下一个空号
- **编号一旦给出就不改**，只改内容不换号
- 新文件加完后更新 README 目录表

---

## 三、内容写作原则

1. **纯知识** — 不写平台专属代码或工具调用指令
2. **可操作** — 写"怎么做"，少写"是什么"。给模板、给例子、给避坑
3. **❌/✅ 对照** — 踩过坑的地方，错误做法和正确做法并排写
4. **表格优先** — 速查信息用表格，不用大段文字
5. **单一事实来源** — 同一知识只写在一个文件里，其他文件引用不要复制粘贴

---

## 四、修改规则

1. 修改实质内容后，更新头部 `> 更新：` 日期
2. 修改后 Git 署名 commit（见 `1-01_用户档案.md` 规则第 5 条）
3. 删除某条知识前，确认没被其他文件引用
4. 项目结束后，项目专属文件 **归档不删除**（编号保留，README 标记"已归档"）
5. **并发防踩**：改文件前先 `git log` 看有没有别人未读的改动。如果发现有人正在维护同一个文件，等对方提交完再动手

---

## 五、防呆铁律（违反必错，必须遵守）

这些都是实际踩坑总结的规则，从根源避免常见问题：

1. **禁止乱备份文件**：所有文件靠Git做版本管理，**不需要单独备份文件**（禁止生成`xxx_v0备份`、`xxx_新版`这类冗余文件），改坏了直接`git restore 文件名`回滚
2. **禁止手改自动生成文件**：所有脚本自动生成的文件（HTML、打包产物、导出文件），必须改源文件重新生成，禁止直接手改生成文件——手改的内容下次生成会被覆盖
3. **进项目先读入项指引**：每个项目根目录必须有`_入项指引.md`，新AI进项目必须先读完这个文件再干活，禁止上来就改
4. **重要信息必须写文件**：重要规则、经验、客户信息必须写入对应MD文件，不能只存在AI的对话记忆里——换AI、换平台不会丢
5. **同一知识只在一处维护**：同一条规则/经验只在一个文件里写，其他地方需要用只写引用链接，禁止复制粘贴——避免多处修改不一致
6. **改完立即提交**：改完文件必须立即Git提交，commit信息必须带AI平台名（如`[豆包] 改了什么`），禁止攒一堆改动一起提交
7. **冲突不擅自决策**：遇到规则冲突、不确定怎么改的地方，不要自己决定，先问用户确认后再改
8. **禁止重复记工作流**：Git就是唯一的工作记录，不需要额外写日志、记改了什么，想查记录直接`git log`

---

## 六、新文件检查清单

- [ ] 头部三行（用途/关键词/更新）齐全
- [ ] 编号不冲突，区间正确
- [ ] README 目录表已登记
- [ ] 内容不与其他文件重复
- [ ] Git 署名 commit 完成

---

## 七、开工必看清单（每次动手前）

> 改任何文件前，先过一遍这 5 条，避免踩坑

| 序号 | 检查项 | 怎么做 | 为什么 |
|:--:|--------|--------|--------|
| 1 | **Git 状态检查** | `git status` + `git log --oneline -3` | 有没有别人未提交的改动/新提交？有没有自己上次忘提交的？ |
| 2 | **读用户档案** | 先读 `1-01_用户档案.md` | 知道用户是谁、偏好是什么、规则是什么 |
| 3 | **读相关规范** | 对应任务读对应文件（见 README 目录表） | 不要凭感觉写，按规范来 |
| 4 | **确认需求** | 跟用户确认清楚要做什么 | 避免做了半天方向错了 |
| 5 | **看建议池/待决策** | 扫一眼有没有相关的建议可以一起做、有没有等用户拍板的事 | 保持闭环，不遗漏 |

### 快速版（30秒）
```
git status + git log -3 → 读 1-01 → 读对应规范 → 开干
```

---

## 八、完工必查清单（做完提交前）

> 改完文件后，过一遍这 8 条，再提交 Git

| 序号 | 检查项 | 怎么做 | 为什么 |
|:--:|--------|--------|--------|
| 1 | **更新日期** | 文件头部 `> 更新：YYYY-MM-DD` 改成今天（**先跑 `date` 确认今天几号，AI 会幻觉日期**；改完后全库搜一遍同日期字符串，防同源多处漏改） | 别人知道这文件什么时候改过；防未来日期笔误 |
| 2 | **格式检查** | 标题层级、表格对齐、链接有效；有生成脚本的跑一遍干跑校验 | 保证可读性；提前发现问题 |
| 3 | **不重复** | 确认内容没有和其他文件重复 | 单一事实来源原则 |
| 4 | **Git 提交** | git add + git commit，署名+写清楚改了什么 | 改完即提交，不攒着 |
| 5 | **更新索引** | 新文件/重大改动更新 README 目录、登记表 | 保持索引最新 |
| 6 | **更新建议池** | 如果做完了某条建议，把状态改成已完成 | 保持建议池闭环 |
| 7 | **同步模板/投影** | 如果本文件是从别处提炼的投影（或被人投影），按映射说明同步对应文件 | 防双源漂移 |
| 8 | **防呆之问** | 这次踩的坑/出的错，值得固化吗？值得→业务坑写进对应文件的踩坑区，系统坑走 `1-09_防呆与持续改善.md` 闭环进台账；不值得→直接提交 | 问题就是改善机会，不让同一个坑绊倒第二个 AI |

### 快速版（30秒）
```
改日期 → 查格式 → git add → git commit（署名+具体内容）→ 更新索引 → 更新建议池 → 防呆之问
```

---

## 九、立项 SOP（防呆版）

> 任何 AI 收到「立项 / 建项目 / 开新方向」类指令，先过本节。**真实事故：某 AI 立项只建了文档没建项目文件夹，被用户纠正后才补**——本节就是把这个坑焊死。

> **立项来源分流（防"通过即直接干"缺口）**：想法池条目（发布类点子，用户认可或达阈值）、建议池条目（体系改进，AI 提 + 群体投票通过 / 用户拍板）——**这两类经确认后先建立项卡（X/P）定范围·验收·负责人·关联工单，再进执行队列，不跳过立项直接干**（建议池通过≠直接执行，详见 `1-09` 记账边界 callout）。**用户直令干具体活免立项**——直接干、`3-01` 加索引即可，与 `_入项指引` 工单免单一致；仅直令涉及**机制层 / 体系级重构**才建立项卡。

### 9.1 第一步：判别立哪种项（防呆之问）

问一句：**这个立项的产出是「规范/机制本身」，还是「一坨要落地的内容/资产」？**

| 类型 | 什么时候 | 落哪 | 要不要文件夹 |
|:--:|------|------|:--:|
| **X 想法立项** | 想法/机制/流程优化——产出就是规范文档、登记行本身 | 想法池/对应规范文件加行 | ❌ 不用 |
| **P 项目立项** | 要有独立产出物（代码/素材/内容库/文档集），可能独立 Git | 【你的项目根目录】`\项目名\` 建文件夹 + 登记表加行 | ✅ 必须 |

**拿不准**：按「有没有一堆文件要攒」判断——要攒文件就是 P 项目；还不确定就问用户一句「这个是想法立项还是项目立项？」

### 9.2 P 项目立项五步（顺序执行）

1. **建文件夹**：【你的项目根目录】`\项目名\`（项目名=文件夹名；英文名=仓库名预留时勿改）
2. **初始化 Git**：`git init` + 首次 commit（放立项文档/README 骨架，署名）
3. **登记**：`3-01_项目与技能登记.md` 加一行（编号、项目名、路径、Git 状态、说明）
4. **更新入口**：README 目录表加指向（让所有 AI 找得到）
5. **署名 commit**：知识库仓库 + 项目仓库各提交一次

### 9.3 P 立项完工必查（提交前自查）

- [ ] 项目文件夹建了？
- [ ] git init + 首次 commit 完成？
- [ ] 登记表加了行？
- [ ] README/入口更新，任何 AI 能找到？
- [ ] 两个仓库都署名 commit 了？

> ⚠️ **铁律：知识库只存地图（登记+规范），不存行李（项目内容）**——立项文档/素材/产出全放项目文件夹，知识库只登记路径和状态（零节核心原则 2）。

### 9.4 X 想法立项三步

1. 想法池/对应规范文件加行
2. 关联工单/规范文件
3. 署名 commit（知识库仓库）
