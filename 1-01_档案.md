# 用户档案

> 用途：所有 AI 助手的统一背景信息 + AI 签名登记表（唯一档案，个人和团队通用）
> 关键词：档案、身份、基本原则、签名、登记
> 更新：【日期】（AI 代填后在此记一笔）

## 基本信息

> 个人版填主人信息；团队版填团队信息（组织/成员/分工）。由 AI 从对话中代填，人核对。

- **称呼：** 【怎么称呼你】
- **职业：** 【你的职业/角色】
- **工作方式：** 【你怎么干活，AI 需要知道的背景，没有可删行】

## 基本原则

### 基础原则（必须遵守）

1. **单一事实来源** — 一条知识只写在一个文件，其他地方引用不复制
2. **地图与行李分离** — 知识库只存"地图"（规范+登记），项目内容（"行李"）在各项目目录自治
3. **Git 是唯一工作记录** — 做完事署名 commit 即完成留痕，不手动重复记账
4. **MD 是事实源，产物是打印件** — 脚本生成的文件（HTML/导出）一律改源文件重新生成，不手改

### 精益原则（做任何事都问一句）

5. **简化** — 能砍则砍，先做最基本，有需要再添加
6. **标准化** — 同类事用同一做法（命名、格式、流程）
7. **Skill化** — 可复用的流程沉淀成 skill，不重复造轮子
8. **自动化（脚本化）** — 重复劳动交给脚本，不靠人肉
9. **防呆** — 会错的地方用机制挡住，不让同一个坑绊倒两次
10. **持续迭代** — 做完复盘，下次更好

> 这 10 条是模板预置的通用默认值，可直接用；不合用的删改随你——改完就是"你的规矩"。

## AI 签名登记表

> 用途：只为签名不重名。**新 AI 入职：在下面加一行你的名字**（用你 commit 时签的名字），原则=避免重复——同平台多个 AI 用"名字+平台"区分。
> 表格末尾保留 `（新AI在此加行）` 占位行；签名后**立即在末尾补回一行占位**，使标记常驻、下一个 AI 不迷路。

| AI 名字（签名用） | 备注 |
|---------|------|
| 【示例：豆包桌面端】 | 【示例：字节 · Win 客户端，登记后可删此行】 |
| （新AI在此加行） |  |

## 改动必签字

任何 AI 修改本知识库或项目文件后，必须立即：

```bash
cd "对应仓库目录"
git add <自己改的文件>
git commit -m "[AI名字] 简述改动内容"
```

> 多 AI 共用工作区时只 add 自己的文件，不用 `git add -A`。
> git author 约定一个统一值（第一个入职的 AI 定下，后续沿用）；AI 身份靠 commit message 的 `[AI名字]` 前缀区分，不要各自改 author 字段，避免历史杂乱。
