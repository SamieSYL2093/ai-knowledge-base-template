# 06 — 自我进化机制（可选高级功能）

> 用途：让知识库不只是一堆死文档，而能从使用中"学乖"——踩的坑自动升级成可信规则、越用越准、不重复踩、不越积越乱
> 关键词：自我进化、经验沉淀、纠正记录、可信度分级、命名空间、知识库自治、不误学
> 更新：2026-07-30
> 适用：想让"踩坑记录"真正变成"不二犯的铁律"、且团队/多AI长期共用知识库时使用；单AI轻量使用可只取「第二节 三阶确认」
> 📌 **与 1-09 的边界**：本文件管「经验可信度分级与治理」；问题→机制的流水线（闭环 5 步、防呆台账）走 `1-09_防呆与持续改善.md`——先走 1-09 闭环，值得长期信的再按本文件分级。
> 来源：机制借鉴自 @ivangdavila/self-improving skill（MIT），吸收为模板规范，零依赖、纯 MD

---

## 一、为什么需要（解决什么痛）

P13 已有「📝 经验永久沉淀」——踩一次坑记下来，所有 AI 自动避开。这解决了"记下来"，但没解决三件事：

- **记的对不对**：单次误记 → 直接变成"铁律"坑人
- **记的重不重**：同一条坑记了 5 遍，版本还不一样，AI 看哪个？
- **积不积灰**：经验库越用越胖，没人收拾，最后谁都不信、谁都不看

自我进化机制就是补这三块：给经验**分级可信度**、给知识**分层继承**、给沉淀**扩容治理**。

---

## 二、核心：经验可信度三阶（最重要）

❌ 不要一次踩坑就当真理。✅ 经验要分级，重复 3 次才升为规则。

| 阶段 | 触发 | 处置 |
|------|------|------|
| **Tentative 暂记** | 第 1 次纠正/发现 | 记到踩坑记录，标注 `(1/3)`，先观察 |
| **Emerging 渐显** | 第 2 次 | 标注 `(2/3)`，留意是不是真规律 |
| **Pending 待确认** | 第 3 次 | 问本人："要永远这样做吗？" 给三选一：总是 / 仅某场景 / 看情况 |
| **Confirmed 已确认** | 本人说"是" | 升级为知识库**正式规则（HOT）**，带确认来源 |
| **Archived 已归档** | 90 天没再用 | 移到归档区，保留不删，注明为何停用 |

**三条铁律：**
1. 3 次重复才升规则，避免从单次误学
2. 不从沉默/一次推断偏好（"他没反对"≠"他同意"）
3. 本人随时可推翻：旧规则归档 + 新规则作暂记，不硬覆盖

---

## 三、命名空间继承（全局默认 vs 项目特例）

知识库天然分层，规则也要分层：

```
global（1-01_用户档案 / 1-02 全局规范）
  └── domain（某类工作，如 20-29_工具技能/）
       └── project（某项目 _入项指引.md）
```

- **最具体者胜**：项目特例覆盖全局默认
- **覆盖写理由**：在文件里用 `## Overrides` 段落写明"哪条被覆盖、为什么"
  ```markdown
  ## Overrides
  - 缩进：空格（覆盖全局的 tab）
  - 理由：本项目 eslint 配置要求空格
  ```
- **冲突检测**：加载时若有矛盾，记一条待审，最具体胜，不擅自改
- **边界**：`## Overrides` 只用于**定本项目的特例**（新增内容）；要**修改别人/全局的现有内容**，仍按 `_入项指引.md` 冲突处理原则——先写明冲突点报备，不擅自改

---

## 四、护栏：什么不学（防误学）

| ❌ 不学 | ✅ 只学 |
|---------|---------|
| 让人顺从更快的模式（操纵） | 本人明确说的偏好 |
| 情绪弱点 | 跨上下文一致的规律 |
| 别人的偏好（"John 喜欢…"不算） | 本人确认过的 |
| 从沉默/单次推断的东西 | 可验证、能讲清理由的 |

> 这条和 P13 防呆铁律「冲突不擅自决策」互补：不确定的经验，先问人，不自己定。

---

## 五、扩容与压实（经验库越用越胖的治理）

| 现象 | 处置 |
|------|------|
| 同一话题 3+ 条相似记录 | 合并成 1 条确认规则（带确认次数范围） |
| 单个经验文件 > 200 行 | 拆子文件或建索引（`index.md` 记行数/更新日） |
| 某条经验写得很啰嗦 | 摘要成一句话（保留关键上下文） |
| 某项目/技能已停用 | 归档带上下文（"为何停用"），不删 |

**压实原则**：合并、摘要、归档，绝不丢已确认的规则和本人的明确纠正。

---

## 六、来源可追溯 + 定期自检（可选）

- **来源可追溯**：每条规则带出处——谁确认、何时、依据（日期 或 `file:line`）。应用规则时引用来源："用 X（来自 1-02_知识库管理规范.md:65）"
- **定期自检（轻量）**：每周/每月扫一遍经验库
  - 没变化 → 不动
  - 有变化 → 只保守整理（追加 / 摘要 / 更新索引），**绝不大改写、绝不删数据**
  - 模板不内置定时，你可用任意平台的 cron 触发这份清单

---

## 七、和 P13 现有机制怎么接

- `20-29_工具技能/` 里的"踩坑记录" = **WARM/COLD 沉淀**（原始经验，暂记级）
- 本文件管 = **HOT 确认规则**（值得信的，已确认级）
- 双仓不冲突：原始坑记在 20-29，确认后的铁律可提到 00/01 或本项目规范
- **跨 AI 共享记忆**：如需多 AI 共用一套进化记忆，把"确认规则"放**仓库内 `shared/` 目录**（Git 版本化、所有 AI 平等可见），**不要**放各家 `~` 目录——Windows 与 WSL 家目录不互通，放 `~` 等于没共享

---

## 八、最小可用（不想全套也行）

单 AI 用户：只取 **第二节 三阶确认** + **第四节 护栏** 两条，立刻有用，零负担。
团队/多 AI：再加上 第三节 继承、第五节 治理、第七节 共享，知识库就真能"越用越聪明"。

---

## 九、新文件检查清单

- [ ] 头部三行（用途/关键词/更新）齐全
- [ ] 经验条目带可信度标注 `(x/3)` 或「已确认」
- [ ] 覆盖全局默认时写了 `## Overrides` + 理由
- [ ] 确认规则带来源（谁/何时/依据）
- [ ] Git 署名 commit 完成
