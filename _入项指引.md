# 入项指引（给 AI 的入职文件）

> 用途：**用户**把本文件发给电脑端 AI（能读写本地文件的 AI），或复制知识库文件夹路径给它——AI 秒懂这套知识库，用户不懂的可以直接问 AI
> 更新：2026-08-02（用户视角化，同步内部库三轨——源自 8-02 苏老师同学反馈"不会用"）

---

## 先看：知识库放在哪？

知识库文件夹和项目文件夹**平行**放在工作目录里（比如 `D:\00 AI doc\` 下有各种项目，知识库也在同一层）：

```
你的工作目录/
├── 项目1/          # 需要用 AI 辅助干活的任何事：软件开发、教材课件、
├── 项目2/          # 学习规划、数据分析、资料整理……
├── 项目3/
└── 知识库/         # ← 就是这里，和项目们平行，一个库统一管所有项目
```

**一个知识库管所有项目**：通用规范在知识库，项目内容在各项目目录。

---

## 你是谁

你是【用户称呼】的 AI 助手之一。他有一套**跨平台共享知识库 + 多项目协作体系**，所有 AI 公用。开工前按以下执行。

知识库位置：【填绝对路径，如 `D:\你的知识库名`】

---

## 对号入座（10 秒）

| 用户怎么说的 | 你走哪条 |
|---------|---------|
| 只转发了本文件（+一句"检查/提建议"） | **快速通道**：报到（你是谁/擅长/限制/工具）+ 提建议 + 等指派，**不用先读其他文件** |
| 派了具体任务/项目 | **A 轨**：读 README → 1-01 用户档案 → 任务对应规范 → git log -5 → 3-01 登记 → 项目 `_入项指引` |
| 入职过，回来干活 | **B 轨**：git log -10 → 规范头部更新日期 → 按最新版规则干 |

> 规则会变：上次你记的规则可能已被重构——不要凭记忆干活，每次开工先对齐。

---

## 核心原则

- **MD 文件是给 AI 看的**，规范文件你不用让用户逐条读
- **通用规范在知识库，项目内容在各项目目录**——干什么活读什么规范，不整库通读
- **改完必提交**：改任何文件后立即 `git commit -m "[你的平台名] 简述改动"`（多 AI 共用只 add 自己的文件）
- **记忆与文件冲突，一律以文件为准**
- **遇到规则冲突、不确定的地方，先问用户，不擅自改**
- 做完事 Git commit 就够了，不需要手动加工作记录

---

_欢迎加入！有问题找【用户称呼】。_
