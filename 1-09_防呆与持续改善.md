# 防呆与持续改善（体系自愈机制）

> 用途：把每个"问题"变成"防呆机制"的闭环流程与台账——让知识库越用越聪明，同一个坑绝不绊倒第二个 AI
> 关键词：防呆、Poka-Yoke、持续改善、踩坑、台账、闭环、L1预防、L2检测、L3固化、名实相符、三态输出
> 更新：2026-08-03（同步 SYL：闭环改 6 步加「减负」第⑥步、新增台账生命周期标注规则、头部瘦身合并历史到 git log。完整历史见 `git log --oneline 1-09_防呆与持续改善.md`）
> 适用：想让"踩坑记录"真正变成"不二犯的铁律"时使用；单 AI 轻量使用可只取「第〇节理念 + 第二节闭环5步」
> 📌 **与 1-06 的边界**：本文件管「问题→机制」的闭环流水线；经验可信度分级（3 次才升规则）与经验库扩容治理见 `1-06_自我进化机制.md`。

> 📌 **记账边界（模板 vs 实例）**：本台账记**你自己踩的坑**（实例级）——它们属于你的系统，随便长，模板升级永远撞不上。如果你发现是**模板机制本身的洞**（入职指引有歧义、对讲机协议会撞车、某条规则放哪个行业都不对），欢迎到 GitHub 提 Issue 回流上游——模板级的改进会随版本发布，惠及所有使用者。**机制随版本走（源头统一发版），台账各自长（本地永久自治），行业包各自填（个性化空间），模板级的洞靠 Issue 回流（细反馈环）。**
> 📌 **两个"向上通道"别混**：① **建议池**（`1-02` 第十节「建议池与待决策」）——只放**你本地**"用户说以后考虑"的暂存项，不对外；② **GitHub Issue**——只放**模板机制本身的洞**，回流上游、惠及所有人。本地临时决定进建议池，模板 bug 才提 Issue。
> 📌 **建议池通过 ≠ 直接执行**：建议池条目经**群体投票通过或用户拍板**后，**不直接执行**——先建立项卡（X/P，见 `1-02` 立项 SOP）定范围·验收·负责人·关联工单，再进执行队列。想法池 / 建议池通过 先立项再干；**用户直令干具体活免立项**（直接干、`3-01` 加索引即可，与工单免单一致），仅直令涉及机制层 / 体系级重构才立项。

---

## 〇、理念（三句话）

1. **问题就是改善机会** — 体系出问题不是坏事，是系统告诉你"这里有个洞"。不追人、不泄气，只问一句：**怎么让它不可能再犯？**
2. **靠人记是最弱的防呆** — 能写进结构的不靠检查，能写进脚本的不靠自觉，能写进规范的不靠记忆。
3. **防呆本身也要防呆** — 只为**真实发生过的问题**或**代价极高的风险**加机制，不为假想的错误加。机制到期要拆（见 `1-02` 机制生命周期），体系只加不减就会变重。

> 🎯 **终极目标**：防呆做在系统里，不做在用户脑子里——**不用刻意研究提示词，很随意的一句话也能指挥 AI，而且不犯错**。实现路径：提示词工程沉淀进系统文件（派活公式 + `_入项指引.md` + 规范 + AI 自检清单），用户唯一需要记的就是那句派活公式；AI 犯了错就走闭环把教训写回系统，系统越来越聪明，用户一直"随意"。

---

## 一、防呆三层次（优先选轻的）

| 层次 | 含义 | 手段 | 例子 |
|:--:|------|------|------|
| **L1 预防** | 让错误**不可能发生** | 单一事实来源+投影、生成器全量渲染、入口标准化、模板化 | 规则只在一个文件维护，其他文件只写指针——想改出两份不一致都做不到 |
| **L2 检测** | 错误发生了**立即发现** | 对账脚本、干跑校验、开工/完工清单、双向核对 | 页码数量 ≠ 页数就 FAIL；脚本退出码非 0 卡住提交 |
| **L3 固化** | 错误变成**组织资产** | 踩坑记录写进规范、根因进本台账、❌/✅ 对照 | 把"字太小"写成字号下限标准 + 踩坑记录 |

> **选择原则：能 L1 不 L2，能 L2 不 L3。** 遇到一个坑先问"能不能从结构上让它不可能再犯"，不行再退到"能不能自动检查发现"，最后才是"写进规范让人记住"。

---

## 二、问题→防呆 闭环（6步）

任何 AI 处理完一个问题后，多走这 6 步（小坑第①②③步一分钟内完成）：

| 步 | 动作 | 说明 |
|:--:|------|------|
| ① | **记录** | 一句话说清问题，别粉饰 |
| ② | **根因** | 问"系统哪里让它可能发生？"——问到结构层为止，**不追人**（AI 犯错 = 规则或结构有洞） |
| ③ | **选层次** | 按上表选 L1/L2/L3，优先轻的 |
| ④ | **落地** | 改规范（业务坑→写进对应业务文件踩坑区）/ 改脚本（可自动化的）/ 改结构（单源化、加指针） |
| ⑤ | **登记** | 系统级防呆补进本文件第三节台账；commit 信息归因问题来源（如"——源自 xx 事故"）。**加行三件套（漏一个=没登记完）：① 台账加行 ② 头部计数 +1（如本文件启用了计数）③ 挂更新行**——源库"加行漏更计数"三次复发后立的规矩，靠自觉记不住，故写进步骤定义 |
| ⑥ | **减负** | 问"这条机制让体系变重了吗？"——加机制时约定到期条件（如"连续4周无变更→拆"），标生命周期标签（🏠永久/🏗️脚手架/👀观察期）；不约定=默认🏠永久。确保「减」和「加」一样有触发、有闭环——治体系只加不减 |

**分流规则**：
- **业务坑**（做某类活踩的）→ 直接写进对应业务文件的踩坑区（`10-19_业务流程/`、`20-29_工具技能/`），不用进本台账
- **系统坑**（协作机制本身的洞）→ 走本闭环，进台账；拿不准怎么改的 → 提建议池（见 `1-02` 第十节）。**通过后先建立项卡（见 `1-02` 立项 SOP）再执行，不跳过立项直接干**

---

## 三、防呆台账（现有机制一览 · 目视化）

> 每个机制都记着"它防什么、从哪个事故长出来的"。审视体系重量时看这里，别凭感觉。

> 🔧 **生命周期标注（2026-08-03 同步 SYL）**：默认🏠永久，不标注。仅对以下机制在名称前标标签——🏗️脚手架（带到期条件）/ 👀观察期（4周后转🏠或拆）。每周整理建议池时顺手扫🏗️标签，到期→进待决策。

| 机制 | 位置 | 防什么 | 层次 | 来源事故 |
|------|------|--------|:--:|----------|
| 【示例】单一事实来源+指针引用 | 1-02 核心原则 | 两处维护同一规则迟早不一致 | L1 | 【某次 A 文件改了 B 文件没改，AI 看旧规则做错】 |
| 【示例】改前先看 git log | 1-02 开工必看 | 并发覆盖别人的改动 | L2 | 【某次两人同时改一个文件互相覆盖】 |
| 【你的第一条机制】 | | | | |

**登记规则**：机制写清五列——叫什么名字、固化在哪个文件、防什么坑、L 几、从哪个事故长的。**没有来源事故的机制不许进表**（那是假想防呆，见第五节边界）。

---

## 四、运转节奏（良性上升机制）

| 频率 | 动作 | 挂在哪里 |
|------|------|----------|
| **每个问题处理后** | 问一句"防呆了吗？"值得→走第二节闭环 | 1-02 完工必查最后一条 |
| **每周整理文件时** | 顺手扫本台账：有没有机制该升级（L3→L2→L1）或该拆 | 每周例行 |
| **每月/成熟后** | 按 `1-02` 机制生命周期审视：脚手架该拆就拆 | 用户主导 |

**上升螺旋**：问题 → 防呆 → 同类问题归零 → 注意力释放给更高级的问题 → 体系能力台阶式上升。

---

## 五、边界（什么时候不防呆）

1. **假想中的错误不防** — 没发生过、代价也不高的，先不建机制，发生了再补（否则体系越来越重）
2. **一次性的意外不防** — 不会重复的偶发事件，记住就行
3. **成本大于收益的不用脚本防** — 写检查脚本半小时、一年省不了三次的，用清单不用脚本
4. 拿不准的 → 提建议池，用户定

---

## 六、防呆手法库

> 第一节 L1/L2/L3 讲的是防呆的**强度**（要防到多硬），本节讲的是防呆的**手法**（具体怎么设计出来）。
> 遇到一个坑不知道怎么防时，拿 6.1 的十条挨个套一遍，通常能找到比"下次注意"强得多的做法。

### 6.1 十大防呆原理 × AI 协作对照表

> 工业防呆（Poka-Yoke）十大原理翻译到 AI 协作场景。用法：从上往下试，**越靠前的越硬、越优先**（大致对应 L1→L2→L3）。

| # | 原理 | 工业含义 | 在 AI 协作里怎么用 |
|:--:|------|---------|------------------|
| 1 | **断根** | 把根源消掉，让错误**不可能**发生 | 单一事实来源+自动投影；生成文件全量重生成禁手改；模板化入口 |
| 2 | **保险** | 多个条件同时满足才放行 | 双闸验收：机器查结构 + 人查内容，缺一不放行 |
| 3 | **自动** | 机器自动检测、异常即停 | 检查脚本退出码非 0，挂进必跑流程，不靠人想起来跑 |
| 4 | **相符** | 数量/形状对不上就装不进去 | 数量对账：A 的个数必须等于 B 的个数（页码数=页数、明细和=总数） |
| 5 | **顺序** | 顺序错就走不下去 | 前置动作没做完不许动笔（开工先 `git status`+`log`，发布前先过检查清单） |
| 6 | **隔离** | 危险区物理隔开 | 高危动作抽走执行权，留给特定角色（对外发布键永远留给人） |
| 7 | **复制** | 同一动作复制多份互相对照 | 多环境/多视角重复验证，取最坏结果（两种屏幕尺寸都测、多 AI 交叉审） |
| 8 | **层别** | 不同类型分开走不同流程 | 按性质分流（业务坑→业务文件踩坑区；系统坑→本台账） |
| 9 | **警告** | 异常时声光提示 | ⚠️ **最弱的一档**：必须撞到人脸上，不能等人去翻（靠人主动打开控制台看的警告 ≈ 没有警告） |
| 10 | **缓和** | 错了也能把损失降到最小 | 让错误可回滚、不可覆盖（追加制不覆盖原文、Git 可回退、只 commit 不 push） |

### 6.2 设计检查机制时的三条铁律

> 源自真实事故：一份产出"四道关全部通过、问题依然存在"——机器查的是"自检代码在不在"却起名"溢出自检"（名实不符）；"这台机器没能力测"被静默算成"通过"（二态遮蔽）；人工只在大屏上看过、小屏全溢出（单点冒充全覆盖）。

**铁律 A · 名实相符** —— 检查项的名字，必须等于它**真实检查的东西**。做不到全覆盖时，**把边界写进名字本身**，而不是写在文档里等人读。
> ✅ 正例：`4.溢出自检JS代码存在(≠页面没溢出)` —— 名字自带免责声明，谁都误读不了
> ❌ 反例：`4.溢出自检JS` —— 读起来像"溢出已查过"，骗过所有 downstream

**铁律 B · 三态输出** —— 验证结果必须区分三态：**通过 / 不通过 / 未验证**。"本机没能力测"永远不许显示成通过，汇总要显著提示、退出码不许是 0。
> 二态系统的致命处：**沉默的能力缺失会伪装成成功**。没测出问题 ≠ 测了没问题。

**铁律 C · 抽样必须写明抽样条件** —— 人工验证要注明环境（屏幕尺寸/浏览器/系统），单一环境的"我看过了"不能当全覆盖。
> 不写环境的"看过了"是无效证据。

### 6.3 「跳过 = 死角」铁律

检查脚本对某类文件/某项"跳过"时：
- **只许跳具体项，不许跳整个文件**——"有几项不适用"被顺手扩大成"整个不检查"，那几项就永远没人管了
- **必须写清跳的是哪几项、为什么**
- **汇总口径必须把"跳过了什么"一起说出来**——否则那句"全过"本身就是误导（名实相符铁律）
- 跳过处还顶着 ✅/⏭️ 的绿灯最危险：**没有检查的人还知道自己心里没底，有假检查的人是真信了**

### 6.4 怎么给一个新坑挑手法（三问）

1. **能不能让它压根发生不了？**（断根/隔离）——能就到此为止，最省事
2. **不能的话，能不能让机器自动发现？**（自动/相符/复制）——**关键：机器必须真的去测，不是查"测试代码在不在"**
3. **还不能的话，才轮到人**（警告/保险）——此时必须回答：**这个提示会撞到人脸上吗？人不看会怎样？**
   > 若答案是"人不看就没事发生"，那这个防呆是假的，退回第 2 问重想。

### 6.5 报问题前先验证（审计纪律）

> 源自真实误报事故：审计时凭旧记忆/旧快照报"有问题"，结果是误报，白折腾一轮。

审计/评审时报"有问题/账实不符"前，必须做到：

1. **确认读的是实时文件** — `git status` 干净 + `git log` 看到最新提交，不是缓存/旧快照/旧的生成文件
2. **算账就算到底** — 逐项核对（例：总分=明细逐行加总），不看"行数多少"就下结论
3. **旧记忆只能当"更仔细核对"的提醒** — 不能当"肯定又有问题"的论据（记忆与文件冲突，以文件为准）
4. **跑不了 git 的平台** — 用户请你审计时，请他贴文件的同时附上 `git log --oneline -1` 的哈希+时间；你回复时**复述该哈希**作为「我已基于此版本审计」的凭证——哈希对不上 = 你看的不是最新版，先对齐再下结论

---

## 七、检查清单

- [ ] 机制有真实来源事故（不防假想错误）
- [ ] 选了最轻的层次（能 L1 不 L2，能 L2 不 L3）
- [ ] 业务坑进了业务文件踩坑区，系统坑进了本台账
- [ ] 检查项名字 = 真实检查的东西（名实相符）
- [ ] "没能力测"显示为未验证而非通过（三态输出）
- [ ] commit 信息归因问题来源（"——源自 xx 事故"）
